version: '2'

services:
  filechecker:
    build: ./filechecker
    restart: unless-stopped
    ports:
      - "8091:8091"
    environment:
      - SPRING_PROFILES_ACTIVE=dev

  downloader:
    build: ./downloader
    restart: unless-stopped
    ports:
      - "8092:8092"
    environment:
      - SPRING_PROFILES_ACTIVE=dev

  sync:
    build: ./sync
    restart: unless-stopped
    ports:
      - "8093:8093"
    volumes:
      - /home/h-yu/Documents/logs:/home/h-yu/logs
      - /mnt:/mnt
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SPRING_DATASOURCE_URL=jdbc:mysql://192.168.20.116:3306/KASSEN
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=K@ssen1225
      - TZ=Asia/Tokyo
